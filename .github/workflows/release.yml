name: Deprecated release workflow

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use tag-driven release instead."
